rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite leitura e escrita apenas para usuários autenticados (seus vendedores)
      allow read, write: if request.auth != null;
    }
  }
}
